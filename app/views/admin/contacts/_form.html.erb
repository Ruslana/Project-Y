<% form_for [:admin, contact] do |f| %>

  <div class="field">
    <%= f.label :some_contact %><br />
    <%= f.text_area :some_contact %>
  </div>

  <div class="submit">
    <%= f.submit "Save" %>
  </div>

<% end %>