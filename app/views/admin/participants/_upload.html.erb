<% form_for([:admin, @participant]) do |form| %>
	
	<div class="field">
		<%= form.label :name %><br />
		<%= form.text_field :name %>
	</div>
	<div class="field">
	    <%= form.label :bio %><br />
	    <%= form.text_area :bio %>
	  </div>
	
	<p><%= form.submit "Upload file" %></p>
<% end %>