<% form_for([:admin, @upload], :html => { :multipart => true }) do |form| %>

	<%= @upload.music_file_name %><br /><br />
	
	<%= form.file_field :file %><br /><br />
		
	<%= form.submit "Upload file" %>

<% end %>