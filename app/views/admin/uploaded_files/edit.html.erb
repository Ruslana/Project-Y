<% form_for([:admin, @uploaded_file], :html => { :multipart => true }) do |form| %>

	<%= @uploaded_file.music_file_name %><br /><br />
	
	<%= form.file_field :file %><br /><br />
		
	<%= form.submit "Upload file" %>

<% end %>