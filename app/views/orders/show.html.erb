<p><h2>Trecks:</h2></p>
<% for track in @order.tracks %>
	<% if @order.complete %>
		<p><%= link_to track.title_full_file, track.audio_file_full.music.url %></p>
	<% else %>
		<p><%= track.title_full_file %></p>
	<% end %>
<% end %>
<% unless @order.complete %>
	<p>Your order total is <%= @order.amount %></p>
	<p><%= link_to image_tag('https://www.paypal.com/en_US/i/btn/btn_xpressCheckout.gif'), :action => 'checkout', :id => @order.secret_hash %></p>
<% end %>