<!-- home body -->
<div id="content-band">
	<% for photo in @photos %>
	<div class="card" style="margin-top:25px;">
		<p class="card-photo"><%= link_to(image_tag(photo.file.url), participants_path) %></p>
	</div>
	<% end %>
	
	<div class="cleaner"></div>
</div>